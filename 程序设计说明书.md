# 华南留学小程序 - 程序设计说明书

## 📋 文档信息

| 项目 | 内容 |
|------|------|
| 产品名称 | 华南留学小程序 |
| 版本号 | v2.0.0-FINAL |
| 发布日期 | 2026-03-27 |
| 版本状态 | **已冻结** |
| 文档类型 | 技术设计文档 |

---

## 一、系统架构

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                      微信小程序前端                          │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │  首页   │  │ 服务页  │  │ 案例页  │  │ 我的页  │        │
│  │ (资讯)  │  │(4分类)  │  │(案例)   │  │(用户)   │        │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘        │
│       └─────────────┴─────────────┴─────────────┘            │
│                         │                                   │
│                   utils/api.js (API封装)                    │
└─────────────────────────┬───────────────────────────────────┘
                          │ HTTPS
┌─────────────────────────┼───────────────────────────────────┐
│                      Node.js后端                           │
│  ┌──────────────────────┴──────────────────────┐           │
│  │              Express 服务器                  │           │
│  │  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐  │           │
│  │  │用户 │ │院校 │ │案例 │ │预约 │ │配置 │  │           │
│  │  │路由 │ │路由 │ │路由 │ │路由 │ │路由 │  │           │
│  │  └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘  │           │
│  │     └───────┴───────┴───────┴───────┘      │           │
│  │                   │                         │           │
│  │            Sequelize ORM                    │           │
│  └───────────────────┬─────────────────────────┘           │
│                      │                                      │
│                   MySQL数据库                               │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 前端框架 | 微信小程序 | 2.30.0+ | 原生框架 |
| 编程语言 | JavaScript | ES6+ | 异步编程 |
| UI样式 | WXSS | - | 类似CSS |
| 后端框架 | Express.js | 4.x | Node.js框架 |
| ORM | Sequelize | 6.x | MySQL ORM |
| 数据库 | MySQL | 8.0 | 关系型数据库 |
| HTTP请求 | axios | 1.x | 后端HTTP调用 |

---

## 二、目录结构

### 2.1 小程序前端目录

```
miniprogram/
├── app.js                  # 小程序入口 (版本已冻结)
├── app.json                # 全局配置
├── app.wxss                # 全局样式
├── config.js               # 环境配置 (版本已冻结)
│
├── components/             # 公共组件
│   └── (业务组件)
│
├── images/                 # 图片资源
│   ├── bg/                 # 背景图
│   ├── icon/               # 图标
│   ├── images/             # 其他图片
│   └── tab/                # Tab栏图标
│
├── pages/                  # 页面目录
│   ├── index/              # 首页
│   ├── service/            # 服务页
│   ├── cases/              # 案例页
│   ├── schools/            # 学校页
│   ├── news/               # 资讯页
│   ├── user/               # 用户中心
│   │   └── favorites/      # 我的收藏
│   ├── appointment/        # 预约相关
│   └── about/              # 关于我们
│
└── utils/                  # 工具函数
    ├── api.js              # API封装
    └── util.js             # 通用工具
```

### 2.2 后端目录

```
huananliu-web/backend/
├── index.js                # 服务入口 (版本已冻结)
├── config/                 # 配置文件
│   └── index.js
│
├── models/                 # 数据模型
│   ├── index.js            # 模型导出
│   ├── User.js             # 用户模型
│   ├── School.js           # 学校模型
│   ├── Case.js             # 案例模型
│   ├── News.js             # 资讯模型
│   ├── Consultant.js       # 顾问模型
│   ├── Appointment.js      # 预约模型
│   └── ...
│
├── routes/                 # API路由
│   ├── auth.js             # 认证路由
│   ├── schools.js          # 学校路由
│   ├── cases.js            # 案例路由
│   ├── news.js             # 资讯路由
│   ├── appointments.js     # 预约路由
│   └── ...
│
├── middleware/             # 中间件
│   └── auth.js             # 认证中间件
│
└── uploads/                # 上传文件目录
```

---

## 三、核心模块设计

### 3.1 用户认证模块

#### 登录流程
```
用户点击登录
    ↓
wx.login() 获取 code
    ↓
wx.getUserProfile() 获取用户信息
    ↓
POST /api/auth/wx-login
    ↓
后端 code → openid (微信接口)
    ↓
查找/创建用户记录
    ↓
返回 token + userInfo
    ↓
本地存储 token + openid
```

#### 手机号绑定流程
```
点击绑定手机号
    ↓
getPhoneNumber 获取 encryptedData + iv
    ↓
wx.login() 获取新 code
    ↓
POST /api/auth/decrypt-phone
    ↓
后端解密获取手机号
    ↓
更新用户记录
    ↓
返回手机号
```

### 3.2 API请求模块

```javascript
// utils/api.js 核心设计

// 基础请求封装
const request = (url, method, data) => {
  return new Promise((resolve, reject) => {
    wx.request({
      url: config.baseUrl + url,
      method,
      data,
      header: {
        'Authorization': 'Bearer ' + token,
        'X-Openid': openid  // 用于用户识别
      },
      success: handleResponse,
      fail: handleError
    });
  });
};

// API 分类
- 首页: getBanners(), getFeaturedCases(), getHotSchools()
- 学校: getSchoolList(), getSchoolDetail()
- 案例: getCaseList(), getCaseDetail()
- 资讯: getNewsList(), getNewsDetail()
- 预约: createAppointment(), getMyAppointments()
- 用户: wxLogin(), decryptPhone(), getUserInfo()
```

### 3.3 数据缓存策略

| 数据类型 | 存储方式 | 过期策略 |
|----------|----------|----------|
| token | wx.setStorageSync | 永久（直到过期） |
| userInfo | wx.setStorageSync | 随token清除 |
| favorites | wx.setStorageSync | 永久 |
| 页面数据 | Page.data | 页面关闭清除 |

---

## 四、数据库设计

### 4.1 核心表结构

#### users 表（用户）
```sql
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  openid VARCHAR(100) UNIQUE NOT NULL,  -- 微信openid
  unionid VARCHAR(100),                  -- 微信unionid
  phone VARCHAR(20),                     -- 手机号
  avatar VARCHAR(500),                   -- 头像URL
  nickname VARCHAR(100),                 -- 昵称
  gender TINYINT,                        -- 性别 0-未知 1-男 2-女
  country VARCHAR(50),
  province VARCHAR(50),
  city VARCHAR(50),
  status TINYINT DEFAULT 1,              -- 状态
  last_login DATETIME,
  created_at DATETIME,
  updated_at DATETIME
);
```

#### schools 表（学校）
```sql
CREATE TABLE schools (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,
  name_en VARCHAR(100),
  country VARCHAR(50),
  city VARCHAR(50),
  logo VARCHAR(500),
  cover VARCHAR(500),
  description TEXT,
  ranking INT,
  is_hot BOOLEAN DEFAULT FALSE,
  status TINYINT DEFAULT 1,
  created_at DATETIME,
  updated_at DATETIME
);
```

#### cases 表（案例）
```sql
CREATE TABLE cases (
  id INT PRIMARY KEY AUTO_INCREMENT,
  student_name VARCHAR(50),
  avatar VARCHAR(500),
  school_id INT,
  major VARCHAR(100),
  gpa VARCHAR(20),
  language_score VARCHAR(50),
  background TEXT,
  experience TEXT,
  offer_school VARCHAR(100),
  offer_major VARCHAR(100),
  is_featured BOOLEAN DEFAULT FALSE,
  status TINYINT DEFAULT 1,
  created_at DATETIME,
  updated_at DATETIME
);
```

#### appointments 表（预约）
```sql
CREATE TABLE appointments (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT,
  service_type VARCHAR(50),
  name VARCHAR(50),
  phone VARCHAR(20),
  content TEXT,
  status ENUM('pending', 'confirmed', 'completed', 'cancelled'),
  created_at DATETIME,
  updated_at DATETIME
);
```

---

## 五、API接口清单

### 5.1 认证接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/wx-login | 微信登录 |
| POST | /api/auth/decrypt-phone | 解密手机号 |
| GET | /api/auth/user-info | 获取用户信息 |
| PUT | /api/auth/user-info | 更新用户信息 |

### 5.2 学校接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/schools | 学校列表 |
| GET | /api/schools/:id | 学校详情 |
| GET | /api/schools/hot/list | 热门学校 |

### 5.3 案例接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/cases | 案例列表 |
| GET | /api/cases/:id | 案例详情 |
| GET | /api/cases/featured/list | 明星案例 |

### 5.4 资讯接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/news | 资讯列表 |
| GET | /api/news/:id | 资讯详情 |
| GET | /api/news/recommended/list | 推荐资讯 |

### 5.5 预约接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/appointments | 创建预约 |
| GET | /api/appointments/my | 我的预约 |
| PUT | /api/appointments/:id/cancel | 取消预约 |

---

## 六、安全设计

### 6.1 认证安全
- 使用微信官方 `wx.login` 获取临时 code
- code 有效期 5 分钟，一次性使用
- 后端换取 openid 和 session_key
- 不将敏感信息存储在前端

### 6.2 数据传输
- 使用 HTTPS 协议
- 敏感字段加密传输
- 手机号通过 `getPhoneNumber` 获取加密数据，后端解密

### 6.3 接口安全
- 需要认证的接口携带 token
- 通过 `X-Openid` 头部识别用户
- 后端验证用户权限

---

## 七、错误处理

### 7.1 前端错误处理
```javascript
// 统一错误处理
try {
  const res = await api.xxx();
} catch (error) {
  wx.showToast({
    title: error.message || '请求失败',
    icon: 'none'
  });
}
```

### 7.2 后端错误响应
```javascript
// 统一响应格式
{
  success: false,
  message: '错误信息',
  error: '详细错误（仅开发环境）'
}
```

---

## 八、性能优化

### 8.1 前端优化
- 图片懒加载
- 分页加载
- 本地缓存
- 减少 setData 次数

### 8.2 后端优化
- 数据库索引优化
- API 响应缓存
- 静态资源 CDN

---

## 九、部署配置

### 9.1 环境变量
```bash
# 后端环境变量
NODE_ENV=production
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=xxx
DB_NAME=huananliu
WX_APPID=xxx
WX_SECRET=xxx
JWT_SECRET=xxx
```

### 9.2 小程序配置
```javascript
// config.js
const DEVELOPMENT_CONFIG = {
  baseUrl: 'http://localhost:3001/api',
  imageBaseUrl: 'http://localhost:3001/uploads',
  debug: true
};

const PRODUCTION_CONFIG = {
  baseUrl: 'https://api.huananliuxue.com/api',
  imageBaseUrl: 'https://api.huananliuxue.com/uploads',
  debug: false
};
```

---

## 十、版本冻结声明

**⚠️ 重要声明**

本文档对应华南留学小程序 v2.0.0-FINAL 版本（2026-03-27），该版本已完成冻结：

1. **禁止修改**核心代码文件：
   - `miniprogram/app.js`
   - `miniprogram/config.js`
   - `huananliu-web/backend/index.js`

2. **禁止修改**数据库表结构

3. 如需修改，必须：
   - 走版本变更流程
   - 创建新的版本分支
   - 更新版本号和文档
   - 重新测试验证

---

## 十一、相关文档

- [产品说明书](./产品说明书.md)
- [后端API文档](./huananliu-web/backend/README.md)
- [部署文档](./DEPLOYMENT.md)

---

**文档版本**: v2.0.0-FINAL  
**最后更新**: 2026-03-27  
**状态**: 已冻结
